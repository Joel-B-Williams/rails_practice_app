<li>
	<%= gravatar_for user, size: 50 %>
	<%= link_to user.name, user %>
	<% if current_user.admin? && !current_user?(user) %>
		| <%= link_to 'Activate Orbital Railguns', user, method: :delete, data: { confirm: "Fire?" }%>
	<% end %>
	<!-- still have to fake the delete method -->
</li>