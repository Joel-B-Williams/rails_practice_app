<% provide(:title, "All users")%>
<h1>All Users</h1>

<!-- automatically looks for an @users variable (based on users controller) -->
<!-- Has a built-in prev/next/numbers, which is why it goes before and after -->
<%= will_paginate %>

<ul class='users'>
	<%= render @users %>
<!-- yeah, you can do that.  Rails looks at the @users variable, sees it's a bunch of users, and puts them all through the _user partial in turn -->
</ul>
<%= will_paginate %>

